import Ember from 'ember';

export default Ember.Service.extend({
	// famFix: function(oldFam) {
	// 	var fam = oldFam;
	// 	if (fam.charAt(0) === '"' && fam.charAt(fam.length -1) === '"') {
	// 		fam = fam.substr(1,fam.length-2);
	// 	}
	// 	return fam;
	// }
});
